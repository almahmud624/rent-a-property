import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#000",
      contrastText: "#000",
    },
    secondary: {
      main: "#6F66F8",
      contrastText: "#6F66F8",
    },
  },
});
